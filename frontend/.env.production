# API calls go through Firebase Hosting rewrites (same-origin), so leave empty.
VITE_API_URL=

# SSE must bypass Firebase Hosting (streaming proxy truncates after ~60s).
# Connect directly to Cloud Run for long-lived SSE connections.
VITE_SSE_URL=https://apexflow-api-j56xbd7o2a-uc.a.run.app
