# API calls go directly to Cloud Run (bypasses Firebase Hosting rewrites which
# can strip/overwrite the client's Authorization header with its own invoker token).
VITE_API_URL=https://apexflow-api-j56xbd7o2a-uc.a.run.app

# SSE must bypass Firebase Hosting (streaming proxy truncates after ~60s).
# Connect directly to Cloud Run for long-lived SSE connections.
# This URL is stable unless the Cloud Run service is deleted and recreated.
# If recreated, update this value with the new service URL from:
#   gcloud run services describe apexflow-api --region=us-central1 --format='value(status.url)'
VITE_SSE_URL=https://apexflow-api-j56xbd7o2a-uc.a.run.app

# Firebase Auth (Web SDK config for project apexflow-ai).
VITE_FIREBASE_API_KEY=AIzaSyCRgSk27aIo4tEzgZvr5oyeTnNbMpm070E
# Use the Firebase Hosting domain (not firebaseapp.com) so signInWithRedirect
# stays same-origin and avoids third-party cookie blocking in modern browsers.
# Firebase Hosting auto-serves /__/auth/handler on any hosting site.
VITE_FIREBASE_AUTH_DOMAIN=apexflow-console.web.app
VITE_FIREBASE_PROJECT_ID=apexflow-ai
VITE_FIREBASE_STORAGE_BUCKET=apexflow-ai.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=807506425655
VITE_FIREBASE_APP_ID=1:807506425655:web:e197331280a9d930080cb2
